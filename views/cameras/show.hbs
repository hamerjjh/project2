{{!-- <div>
    Name: {{camera.name}} - Price: {{camera.price}} - Color: {{camera.color}} - Lens Size: {{camera.lensSize}}
    <a href="/brands/{{brandId}}/cameras/{{camera._id}}/edit">Customize Camera</a>
    <a href="/brands/{{brandId}}/cameras/{{camera._id}}/delete">Delete Camera</a>
</div> --}}

{{!-- <img src="{{camera.img}}" alt=""> --}}



<main>

	<h2>You are checking out the:

		{{camera.name}}

	</h2>

	<div class="camera-container-show">
		<h2 id="camera-name">
                Name:
				{{camera.name}}

		</h2>

          <img src="{{camera.img}}" class='img' alt="">  
		<br>
		<h3>Color:</h3>
			<p>

				{{camera.color}}

			</p>
		<h3>Price:</h3>
			<p id="cost">$

				{{camera.price}} </p>
        <h3>Lens Size:</h3>
			<p id="lensSize">

	 				{{camera.lensSize}} mm

			</p>

			<br>   
            <a href="/brands/{{brandId}}/cameras">Return to Cameras Page</a>
			<a href="/brands/{{brandId}}/cameras/{{camera._id}}/edit">Customize Camera</a>
            <a href="/brands/{{brandId}}/cameras/{{camera._id}}/delete">Delete Camera</a>

		</div>

		
</main>